export const sum = (augend: number, addend: number): number => {
  return augend + addend;
};

export const difference = (minuend: number, subtrahend: number): number => {
  return minuend - subtrahend;
};
